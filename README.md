
  # esta es (copia)

  This is a code bundle for esta es (copia). The original project is available at https://www.figma.com/design/agHZkYYgYofWSvxZyXhcGO/esta-es--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  